# ── 依赖 ──
node_modules/

# ── 构建产物（exe / 解包目录，由 build.js 生成） ──
dist/
backups/

# ── 日志 ──
*.log

# ── 操作系统 / 编辑器 ──
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini
*.swp
*~
.vscode/
.idea/

# ── 本地运行时配置（用户配置在用户主目录 ~/.momoyu-widget，不入库） ──
.momoyu-widget/
config.json

# ── 临时 / 一次性脚本 ──
_rename.js
admin-window-shot.js
extract-winCodeSign.py

# ── 截图（非源码资源） ──
*-shot.png
admin-window.png
widget-crop.png

# ── 打包缓存 ──
out/
.cache/
